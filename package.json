{
  "name": "train-yahoo-jp",
  "version": "0.0.1",
  "private": true,
  "description": "train information from yahoo.co.jp",
  "scripts": {
    "build": "babel src --out-dir lib --source-maps-inline",
    "watch": "babel src --out-dir lib --watch --source-maps-inline",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Motoki Kobayashi <otofu.xxx@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "cheerio-httpcli": "^0.6.8"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.10.1",
    "babel-core": "^6.10.4",
    "babel-preset-es2015": "^6.9.0"
  }
}
